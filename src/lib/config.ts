/** Constantes de comportamiento global, editables en un único sitio. */

// Cursor personalizado (círculo blend-mode difference). Desactivable aquí.
export const ENABLE_CUSTOM_CURSOR = true

// Tilt 3D en las tarjetas de los actores. Desactivable aquí.
export const ENABLE_TILT = true

// Easing base del sistema (expo-out) para Framer Motion.
export const EASE_EXPO = [0.16, 1, 0.3, 1] as const
